// TODO: Modify so that it uses env vars instead of hardcoded values

const CognitoAuthConfig = {
  userPoolId: 'USER POOL ID HERE',
  clientId: 'CLIENT ID HERE',
  region: 'us-east-2',
};

export default CognitoAuthConfig;
